import 'package:flutter/material.dart';

class SideQuests extends StatelessWidget {
  const SideQuests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
